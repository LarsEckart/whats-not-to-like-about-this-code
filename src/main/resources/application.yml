

datasources:
  default:
    url: jdbc:h2:mem:delivery;LOCK_TIMEOUT=10000;DB_CLOSE_ON_EXIT=TRUE
    driverClassName: org.h2.Driver
    username: sa
    password: ''
    schema-generate: CREATE_DROP
    dialect: H2

flyway:
  datasources:
    default:
      enabled: true
